import React from 'react';
import LoginContainer from '../../containers/LoginContainer';

const Login = () => {
        return (
                <main>
                <LoginContainer/>
                </main>
        );
    
};

export default Login;
